{
  "buildCommand": "cd frontend && npm install && npx vite build && cd .. && node -e \"const fs=require('fs'); fs.rmSync('dist', {recursive:true, force:true}); fs.mkdirSync('dist'); fs.cpSync('frontend/dist', 'dist', {recursive:true});\"",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
