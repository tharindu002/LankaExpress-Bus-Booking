{
  "buildCommand": "npm --prefix Admin run build && node -e \"const fs=require('fs'); fs.cpSync('Admin/dist', 'dist', {recursive: true});\"",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
